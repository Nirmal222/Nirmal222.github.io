import { Box, Button, Flex, Text } from "@chakra-ui/react";
import React from "react";

const Navbar = () => {
  return <Box as={Flex} w="100vw" h="4.5rem" bg="red"></Box>;
};

export default Navbar;

// styles
